<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Product2__c</fullName>
    <label>Product</label>
    <type>Lookup</type>
    <referenceTo>Product2</referenceTo>
    <relationshipLabel>Stock Reservations</relationshipLabel>
    <relationshipName>Stock_Reservations</relationshipName>
    <required>false</required>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The product whose stock is reserved. Not required + SetNull (Product2 cannot parent a cascade/restrict lookup; required lookups demand cascade/restrict). Population enforced by app logic. See ADR-0006 / materialization finding.</description>
</CustomField>
